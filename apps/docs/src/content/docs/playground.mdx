---
title: Playground
description: Try IntentForm live in the browser.
---

Try IntentForm live. Enter an intent below — the form will pre-fill based on what you typed.

import Playground from '../../components/playground'

<Playground client:load />

:::note[API key]
The playground runs client-side. Enter your OpenAI API key in the field above — it stays in your browser session only and is never sent anywhere except directly to the OpenAI API.

For production use, keep keys server-side. See [Keep API Key Server-Side](./recipes/server-side-key/).
:::
